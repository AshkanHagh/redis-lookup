{
  "name": "redis-lookup",
  "version": "0.1.2",
  "description": "Redis Lookup is a high-performance search package designed to perform rapid searches on user-defined datasets using Redis. By leveraging the capabilities of Redis and Upstash, this package provides efficient search functionalities, such as finding users or products based on partial matches. It is particularly useful for applications requiring quick lookup and autocomplete features.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "repository": {
    "url": "https://github.com/AshkanHagh/"
  },
  "scripts": {
    "build": "tsup"
  },
  "keywords": [
    "redis",
    "@upstash/redis"
  ],
  "author": "haghdoost",
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@upstash/redis": "^1.32.0"
  }
}
